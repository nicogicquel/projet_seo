{% extends 'backlinks.html.twig' %}

{% block title %}
    Liste des topics
{% endblock %}

{% block section %}

    <div class="formHead">
        <h1>Liste des topics</h1>
        <div class="buttonAdd">
            <a href="{{ path('topical_new') }}">Ajouter un nouveau topic</a>
        </div>
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for topical in topicals %}
            <tr>
                <td><a href="{{ path('topical_edit', { 'id': topical.id }) }}">{{ topical.nom }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('topical_edit', { 'id': topical.id }) }}">Modifier</a>
                        </li>
                        <li>
                            <a href="{{ path('topical_edit', { 'id': topical.id }) }}">Supprimer</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="buttonAddBottom">
        <a href="{{ path('topical_new') }}">Ajouter un nouveau topic</a>
    </div>
{% endblock %}
