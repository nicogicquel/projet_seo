{% extends 'base.html.twig' %}

{% block body %}
    <h1>Topical</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ topical.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ topical.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('topical_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('topical_edit', { 'id': topical.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
